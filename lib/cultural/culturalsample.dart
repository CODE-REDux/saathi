import 'package:flutter/material.dart';
import 'package:appbar/slidingpanel.dart';

class Culturaldance extends StatelessWidget {
  // This widget is the root of your application.
  @override
  Widget build(BuildContext context) {
    return MyPanel(
      colour: Colors.red,
      teaxt: "Dance Freakz",
    );
  }
}

class Culturalfinearts extends StatelessWidget {
  // This widget is the root of your application.
  @override
  Widget build(BuildContext context) {
    return MyPanel(
      colour: Colors.blue,
      teaxt: "Fine Arts",
    );
  }
}

class Culturalindianmusic extends StatelessWidget {
  // This widget is the root of your application.
  @override
  Widget build(BuildContext context) {
    return MyPanel(
      colour: Colors.amber,
      teaxt: "Indian Music",
    );
  }
}
// import 'package:flutter/material.dart';
// import 'package:fluttertrial2/slidingpanel.dart';

class Culturalliterary extends StatelessWidget {
  // This widget is the root of your application.
  @override
  Widget build(BuildContext context) {
    return MyPanel(
      colour: Colors.green,
      teaxt: "Literary",
    );
  }
}

class Culturaldrama extends StatelessWidget {
  // This widget is the root of your application.
  @override
  Widget build(BuildContext context) {
    return MyPanel(
      colour: Colors.orange,
      teaxt: "Masquerades",
    );
  }
}

class Culturalquiz extends StatelessWidget {
  // This widget is the root of your application.
  @override
  Widget build(BuildContext context) {
    return MyPanel(
      colour: Colors.red,
      teaxt: "Quiz",
    );
  }
}

class Culturalwesternmusic extends StatelessWidget {
  // This widget is the root of your application.
  @override
  Widget build(BuildContext context) {
    return MyPanel(
      colour: Colors.blue,
      teaxt: "Western Music",
    );
  }
}
