import 'package:flutter/material.dart';
import 'package:appbar/slidingpanel.dart';

class Sntcamc extends StatelessWidget {
  // This widget is the root of your application.
  @override
  Widget build(BuildContext context) {
    return MyPanel(
      colour: Colors.red,
      teaxt: "AeroModelling Club",
    );
  }
}

class Sntcastronomy extends StatelessWidget {
  // This widget is the root of your application.
  @override
  Widget build(BuildContext context) {
    return MyPanel(
      colour: Colors.blue,
      teaxt: "Astronomy Club",
    );
  }
}

class Sntcbusiness extends StatelessWidget {
  // This widget is the root of your application.
  @override
  Widget build(BuildContext context) {
    return MyPanel(
      colour: Colors.amber,
      teaxt: "Business Club",
    );
  }
}

class Sntccops extends StatelessWidget {
  // This widget is the root of your application.
  @override
  Widget build(BuildContext context) {
    return MyPanel(
      colour: Colors.lightGreen,
      teaxt: "Club of Programmers",
    );
  }
}

class Sntccsi extends StatelessWidget {
  // This widget is the root of your application.
  @override
  Widget build(BuildContext context) {
    return MyPanel(
      colour: Colors.orange,
      teaxt: "Club of Sustainability and Innovation",
    );
  }
}

class Sntcrobotics extends StatelessWidget {
  // This widget is the root of your application.
  @override
  Widget build(BuildContext context) {
    return MyPanel(
      colour: Colors.red,
      teaxt: "Robotics Club",
    );
  }
}

class Sntcsae extends StatelessWidget {
  // This widget is the root of your application.
  @override
  Widget build(BuildContext context) {
    return MyPanel(
      colour: Colors.blue,
      teaxt: "Society of Automobile Engineers",
    );
  }
}
